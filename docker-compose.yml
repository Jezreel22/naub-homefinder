services:
  postgres:
    image: postgres:16
    container_name: naub-pg
    environment:
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - naub-pg-data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  naub-pg-data: